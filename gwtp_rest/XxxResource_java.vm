/*
 * Created on $today.date ( Date ISO $today.date("yyyy-MM-dd") - Time $today.time )
 * Generated by Telosys ( http://www.telosys.org/ ) version $generator.version
 */
package ${target.javaPackageFromFolder(${SRC})};

#set( $entityClass = "${entity.name}Dto" )
#set( $uncapitalizedEntityName = $fn.uncapitalize($entity.name) )

import java.util.List;

import javax.ws.rs.Consumes;
import javax.ws.rs.DELETE;
import javax.ws.rs.GET;
import javax.ws.rs.POST;
import javax.ws.rs.PUT;
import javax.ws.rs.Path;
import javax.ws.rs.PathParam;
import javax.ws.rs.Produces;
import javax.ws.rs.core.MediaType;

import ${ROOT_PKG}.shared.dto.${entityClass};
import ${ROOT_PKG}.shared.dto.common.ListItemDto;

#set( $keyPart = "")
#foreach( $field in ${entity.attributes} )
#if( $field.isKeyElement())
#set( $uncapitalizedFieldName = $fn.uncapitalize($field.name))
#set( $keyPart = "${keyPart}/{${uncapitalizedFieldName}}")
#end
#end 
## --- Path
#set( $path = $fn.uncapitalize($entity.name) )

## --- Pk arguments
#set( $pkArgs = "" )
#foreach( $attribute in $entity.keyAttributes )
#if( $foreach.count > 1 )
#set( $pkArgs = "${pkArgs}, " )
#end
#set( $pkArgs = "${pkArgs}${attribute.name}" )
#end
## --- Primary Key request path, e.g. "{customerCode}/{bookId}"
#set( $pkPath = "" )
#foreach( $attribute in $entity.keyAttributes )
#if( $foreach.count > 1 )
#set( $pkPath = "${pkPath}/" )
#end
#set( $pkPath = "${pkPath}{${attribute.name}}" )
#end
## --- Primary Key Path Variables, e.g. "@PathParam("customerCode") String customerCode, @PathParam("bookId") Integer bookId"
#set( $pkPathVariables = "" )
#foreach( $attribute in $entity.keyAttributes )
#if( $foreach.count > 1 )
#set( $pkPathVariables = "${pkPathVariables}, " )
#end
#if(${attribute.type} == 'Short' || ${attribute.type} == 'Long')
#set( $pkPathVariables = "${pkPathVariables}@PathParam(${QUOT}${attribute.name}${QUOT}) Integer $attribute.name" )
#elseif(${attribute.type} == 'Float' || ${attribute.type} == 'BigDecimal')
#set( $pkPathVariables = "${pkPathVariables}@PathParam(${QUOT}${attribute.name}${QUOT}) Double $attribute.name" )
#else
#set( $pkPathVariables = "${pkPathVariables}@PathParam(${QUOT}${attribute.name}${QUOT}) $attribute.type $attribute.name" )
#end
#end

@Path("/${path}")
@Produces(MediaType.APPLICATION_JSON)
@Consumes(MediaType.APPLICATION_JSON)
public interface ${entity.name}Resource {

	@GET
	public List<${entityClass}> findAll();

	@GET
	@Path("${pkPath}")
	${entityClass} get(${pkPathVariables});

	@POST
	void create(${entityClass} ${uncapitalizedEntityName});

	@PUT
	@Path("${pkPath}")
	void update(${entityClass} ${uncapitalizedEntityName}, ${pkPathVariables});

	@PUT
	void save(${entityClass} ${uncapitalizedEntityName});

	@DELETE
	@Path("${pkPath}")
	void delete(${pkPathVariables});

	@GET
	@Path("/${uncapitalizedEntityName}s-list-items")
	List<ListItemDto> listItems();
}
